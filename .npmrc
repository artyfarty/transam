# Supply-chain hardening: refuse packages published less than 3 days ago.
# (Honored by pnpm >= 9.15 / 10. Ignored harmlessly by older pnpm; we also
# pin exact, well-aged versions in package.json as the real safeguard.)
minimum-release-age=4320
